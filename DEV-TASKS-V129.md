# 《墨问岐黄》开发任务 V129（夜读医典短环）

日期：2026-09-16  
下发：全局统筹 → 开发负责人 → 小组  
前置：V128 已验收；一日已有诊→采→炮→复诊钩子；尚无阁楼夜读  
依据：设定「夜读医典解锁理论节点」；阁楼放医典/绘卷；夜格耗时；十问歌/脉名可夜读后显；文化学习声明

## 目标

在可玩日接上 **夜读短环**：

暮/诊事告一段落 → 上 **阁楼** → 翻 1～3 张医典残页 → 解锁对应 **理论节点** → 次日（或当夜结束）在诊脉/十问/穴位上看到浅层增益（名词或短提示），不是数值开挂。

手感：残烛翻页、小楷落下；学到的是「能叫出名/多一句原典」，不是背题库通关。

## 范围（锁死）

- **地点**：杏林小筑阁楼（诊室/厢房入口即可）。不要五方、不要穿越绘卷副本。
- **时辰**：耗 **夜** 格 1（与炮制暮格并列）；无夜格则提示明日再来或允许「提前歇息进夜」。
- **医典切片（先 3 页，可扩展槽）**：
  1. **寒热虚实**（理论入门）→ 解锁节点 `theory.hanre_xushi`：结算/问舟旁可显示四字直觉短注（非诊断名）。
  2. **十问歌 · 残句**（连张景岳钩，切片只一页）→ `theory.tenq_song`：十问按钮旁可显一句原典（中文+译）；未解锁仍只短问。
  3. **脉语 · 弦/浮**（选一）→ `theory.pulse_names`：脉象小游戏形容词旁可显示标准脉名（未解锁只给「如按琴弦」类描写）。
- **阅读操作**：点页 → 短文（中英日）→「领悟」确认 → 写入 `play.codex_unlocked[]`；已读页可复习不耗夜格（或半价，切片可免费复习）。
- **苏问舟**：可有 1 句阁楼旁白（疑问或点到为止），不讲标准方。
- **存档**：`play.codex_unlocked` + 可选 `play.theory_nodes`；重进保留。
- **声明**：医典页脚复用文化学习、不能替代就医键。
- 美术：明快 Q；宣纸页、残烛、阁楼一角；不要恐怖经卷。

## 完成标准

1. 从诊室能进阁楼夜读，再返回；耗夜格（或等价日终入口）。
2. 至少 3 页可读，读完解锁节点；UI 上能看出「解锁前/后」差异（十问旁原典或脉名至少一处）。
3. 复习已读页不重复扣夜格（或按任务书约定）。
4. 冒烟：`SMOKE PASS` + `codex_night_ok`（进阁楼 → 解锁一页 → 存档含节点）。
5. 推 main；LLM/secrets/炮制针灸逻辑不改。

## 分工

### 开发负责人
- 转发；定入口与夜格扣法；验收解锁前后差异；推送。

### 游戏逻辑设计
- `codex` / `theory_nodes` 表；解锁条件（读完即解锁，切片无复杂前置）。
- 夜格消耗；与 `play.day` 衔接（夜读后可接次日开馆）。
- 十问/脉象读取解锁旗显原文或脉名。

### 剧本设计
- 3 页正文（中英日，短）；领悟反馈；问舟阁楼 1～2 句；页脚声明键。
- 不写可替代就医的方药医嘱。

### 游戏美工设计
- 阁楼背景一角；医典 UI（封面/残页）；解锁小印「识」。
- 风格跟现行明快 Q。

### 游戏场景设计
- 阁楼入口热区；书桌/书架交互点；CAM_LOFT。

### 游戏角色开发
- 夜读场景/状态机；解锁写入；十问/脉象读旗；冒烟。

### 其他要素
- i18n：`CODEX_*` / `THEORY_*` / `NIGHT_READ_*`。
- 音效：翻页、残烛 1～2 条（可复用 ui-ink）。

## 不做

完整墨问医典五分册、杏林谱名医课树、铜人无限练、穿越绘卷、情缘、默写考试强制、改 Steam、J/T。

## 回我

手操或截图：进阁楼读一页 → 次日十问/脉象见解锁痕迹 + commit。
