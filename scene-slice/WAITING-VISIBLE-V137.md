# V137 · 候诊立绘可见 / 热区确认（不重绘医馆）

日期：2026-09-24  
角色：场景设计  
依据：`DEV-TASKS-V137.md` §F、`DEV-TASKS-V137.1.md` §B、`PORTRAIT-LAYOUT.md`、`WAITING-SLOT4-V133.md`  
对照：`incoming/clinic-overlay-day2-haopeng-20260924.png`（顶栏四张文字牌 + 底引导箱；**场景中部无人**）

**结论：A–D 脚底与热区坐标保持锁死，不必为挪牌而改世界坐标；不做整张医馆重绘。**

示意：`scene-slice/waiting/WAITING_VISIBLE_V137.png`（镜 `ui/waiting/`）。

---

## 0. 对照结论（day2 叠图）

| 观察 | 场景含义 |
| --- | --- |
| 候诊「牌」已在 **屏幕顶 HUD 条**（四张米色卡） | 名单牌 ≠ 世界凳上立绘；牌挪开后 **更需要** L5 凳上 Sprite |
| 中部暖木柜台区空 | 世界 Y≈290～340 一带 **未被** 顶牌/底旁白盖死 → 脚底坐标仍落在可点可读区 |
| 底左引导箱、右上 Nav | 与 A–D 世界脚底无硬冲突；立绘头上勿再叠第二套世界木牌 |

顶牌挪走后：**不改** A–D 世界坐标；角色开发保证 `_apply_portraits` 挂单人图。

---

## 1. 锁死脚底（与 PORTRAIT / V133 **不变**）

| 槽 | 节点 | 脚底 position | 显示高 |
| --- | --- | --- | --- |
| A | Patient0 / BenchA | **(405, 290)** | 候诊约 **240～280**（坐诊 280～320 的 ~0.85；须一眼可认） |
| B | Patient1 / BenchB | **(545, 290)** | 同上 |
| C | Patient2 | **(300, 340)** | 同上 |
| D | Patient3 | **(685, 290)** | 同上 |
| 坐诊 | `_seat_patient` | **(1040, 590)** | 280～320 |
| 江晚 | Apprentice | **(1100, 720)** | ~260；须可见 |

脚底规则：`sprite.position = Vector2(-w/2, -h)`；Y-sort 开；**禁止** `STRETCH_KEEP_ASPECT_COVERED` 裁脚。

---

## 2. 点选热区（可点确认）

圆热区 **r=90**（`Circ_bench`），圆心建议略高于脚底（凳面）：

| 槽 | 建议 Area 中心 | 文档 rect 兜底 | 现状（clinic.tscn） |
| --- | --- | --- | --- |
| A | **(405, 255)** | (360, 220, 90, 70) | `Area_候诊凳A` ✅ |
| B | **(545, 255)** | (500, 220, 90, 70) | `Area_候诊凳B` ✅ |
| C | **(300, 305)** | (255, 270, 90, 70) | **缺 `Area_候诊凳C`** → 角色开发补一枚，shape=`Circ_bench` |
| D | **(685, 255)** | (640, 220, 90, 70) | `Area_候诊凳D` ✅ |

开诊路径：点凳 Area / 点立绘 Sprite / 点顶 HUD 牌 → 同一 handler（逻辑侧已有则对齐）。

**坐标微调许可**：仅当实测被新 HUD 裁切时，单槽可 ±20 px；默认 **零改**。本确认包 **不**建议移动。

---

## 3. 可见性（给角色 / 美工）

1. **停用合图依赖**：`L5_characters/Patients` 旧合图 Art 在单人 `Patient0–3` 启用后应 `visible=false` 或移除挂载，避免空凳/盖人。  
2. 每席按 `current` 池 id `load` `ui/characters/*.png`；空席不残留图。  
3. **z**：L5（5）高于底图；顶 HUD / 对话 CanvasLayer 不整块盖死凳区（顶牌已在上沿，OK）。  
4. 顶文字牌可留作名单，**不能替代** 凳上立绘。  
5. 不做整张 Q 版医馆重绘；底图可暂留现药柜图。

---

## 4. 明确不做

- 新大地图、改六机、改坐诊 (1040,590)、改 A–D 默认脚底  
- 整馆重绘、十二印墙、新 WAIT_E

---

## 5. 交接

### 场景（本包）
- 确认：牌挪顶后 A–D **仍可点可读**；坐标锁死；缺 C 热区已标出。

### 角色开发
- `_apply_portraits` 四席单人图；补 `Area_候诊凳C`；关合图；冒烟 `ui_boot_portrait_ok` / 立绘可见手操。

### 美工
- 立绘脚底贴底；候诊缩放下仍可认脸。
