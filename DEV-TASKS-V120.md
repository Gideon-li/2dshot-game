# 《墨问岐黄》开发任务 V120（收圆可玩日）

日期：2026-09-06  
下发：全局统筹 → 开发负责人 → 小组  
前置：V119 已验收并推送 `01b3fed`  
依据：设定包 v1.19；本期不扩大到情缘/穿越/全表药材

## 目标

把「杏林小筑可玩日」从冒烟可过，收成 **手操也顺** 的一圈：

开馆免责 → 接阿福（或三案之一）→ 四诊 + 十问 → **缺问时苏问舟真的出旁白** → 开方或扎针（反畏仍硬锁）→ 看到对证分 M / 疗效 → **一句回访或病程钩子** → 可接下一位。

## 完成标准

1. 手操缺「寒热」或「汗」时，苏问舟冒出疑问句旁白（不报方不报证）；补问后可出「这一问有了。」同诊 ≤2 句。
2. 小荷至少在候诊条或药柜报警时说一句 UI 口吻（仍不代诊、不可点）。
3. 结算屏能看懂 M（或快/稳/准/温里与 M 对齐的展示），合法路径向愈，反畏拖药仍弹回并给短因。
4. 治疗后有回访/病程一句（可本地模板，不必接 LLM）。
5. `./run_godot.sh --headless res://scenes/smoke.tscn` 仍 PASS；新增旁白冒烟（可另加 smoke 断言）。
6. 推到 `Gideon-li/2dshot-game` main；`secrets.env` 不进库。

## 分工（由你转发）

### 开发负责人
- 对齐本文件与仓库现状；验收手操一圈；推送。
- 拦范围：不做情缘、刁难全开、气机透视、节气/地域乘数实装（J=T=1 保持）。

### 游戏逻辑设计
- 接线：四诊旗 + 十问覆盖 → 触发 `mentor_cue`；同诊计数。
- 结算展示字段与 `evaluate()` 对齐；回访钩子数据结构（下一次进馆可读）。
- 小荷代诊仍不做；只留「馆规：低危可代」注释/开关默认关。

### 剧本设计
- 确认三案 `mentor.case_cues` / `exam_missing_cues` 中英日每条都是疑问句。
- 小荷候诊/药柜各 ≥3 句短口吻；回访一句/案。
- 禁止问舟说诊断名、方名。

### 游戏角色开发
- 运行时真正弹出问舟旁白（现在数据在、要保证 UI 可见）。
- 小荷 UI 气泡；结算与回访表现。
- Helix 保持；失败回退。

### 游戏美工设计
- 问舟旁白气泡、小荷小表情/角标（极简即可）。
- 若 CAM_ASK 仍是旧图，按江晚+现行病人补一版。

### 游戏场景设计（按需）
- 仅当旁白/候诊锚点缺热区时补；相机与热区默认不动。

### 其他要素 / 音乐（按需）
- 旁白出现轻音（已有 chime/stamp 可复用）；i18n 键补齐旁白与小荷句。

## 不做

情缘谱、刁难再诊池、气机观、J/T 实装、600/500 全表、Steam 上架操作、扩大地图。

## 回我

手操一圈录 30–60 秒或给商店新截图 + commit。缺口只报阻塞。
