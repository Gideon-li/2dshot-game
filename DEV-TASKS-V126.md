# 《墨问岐黄》开发任务 V126（角色立绘）

日期：2026-09-08  
下发：全局统筹 → 开发负责人 → 小组  
前置：V125 已关；诊室已有 `ui/apprentice.png`、`ui/patients-three.png` 合图与 L5 层  
依据：`patients/SCRIPT.md` / `slice_characters.json` `art_hint`；画风锁明快 Q 版；学徒默认 **江晚**（禁止画回林晚）

## 目标

把切片里「能看见的人」从合图/占位，升成 **可单独挂载的立绘**，问诊与诊室一眼能分清是谁：

- **必做 4 人**：江晚（`apprentice_jiang`）、赵阿福（`char_porter`）、沈清荷（`char_clerk`）、周婆婆（`char_copyist`）
- **选做**：小荷（UI 口吻位，半身即可）；苏问舟本期可不进可点热区，有旁白条小头像加分，不做也可

手感：坐堂/问诊时当前病人立绘切换清楚；江晚在诊室锚点稳定，不抢病人戏。

## 范围（锁死）

- 风格：明快 Q 版，圆头身、表情清楚、颜色干净；中式医馆气质。不要写实解剖、不要暗黑病院、不要林晚命名/造型回潮。
- 规格（建议，可微调但要统一）：
  - 每人一张 **透明底 PNG** 立绘（推荐约 512～768 高，或同比例系列）
  - 路径建议：`ui/characters/<id>.png`（如 `jiang_wan.png` / `zhao_afu.png` / `shen_qinghe.png` / `zhou_popo.png`）
  - 可选：每人 1 张表情差分（平静 / 不适）——有则挂，无则单张也过验收
- 外形跟 `art_hint`：
  - 江晚：浅墨学徒袍，不抢戏
  - 阿福：短褐、肩宽、风吹感、坐不住
  - 清荷：青灰衙役常服、束发、肩往里含
  - 婆婆：深褐宽袖、白发髻、手瘦
- 接入：
  - 诊室座位 / 问诊 HUD：按 `current_patient_id` 换对应立绘（不要再靠一张三人合图硬裁当唯一方案；合图可留作商店/宣传）
  - 江晚：诊室学徒锚点用新立绘替换或叠加清晰单人图
- 旧 `patients-three.png` / `apprentice.png`：可保留作备用或 Steam 构图，但运行时优先读单人文件。
- 文案/逻辑/LLM/炮制/针灸：**不改玩法**，本档以美术+挂载为主。

## 完成标准

1. 4 张必做立绘进库，风格统一，能辨认身份。
2. 诊室或问诊中切换三病人时，立绘正确切换；江晚锚点为江晚。
3. 无色块占位；透明边不脏；明快 Q 版一致。
4. 冒烟：`SMOKE PASS`（可加 `portrait_swap_ok`：三病人各能加载贴图）。
5. 推 main；`secrets.env` 不进库。

## 分工

### 开发负责人
- 转发；定路径与命名；验收挂载与辨识度；推送。

### 游戏美工设计（主责）
- 画 4 张必做立绘（+可选小荷/问舟小头像）；更新 `ui/ART.md` 索引。
- 若需表情差分，优先病人「不适」一张。

### 游戏角色开发
- 诊室/问诊按 id 加载 `ui/characters/*.png`；缺图时回退旧合图并打日志，不崩。
- 冒烟检查四路径存在且可 `load`。

### 游戏场景设计
- 座位/对话位框适配新立绘比例（脚底对齐、不挡四诊按钮）。

### 剧本设计
- 一般不动；若立绘旁要短身份条，复用现有 `hud` 名，不新写诊断。

### 游戏逻辑设计
- 可不改；若加 `art.portrait` 字段到 characters JSON，给路径即可。

### 其他要素（按需）
- 无新玩法键；`ART.md` / 资源表更新即可。

## 不做

全员表情系统、换装、苏问舟可点师傅 HUD、小荷代诊、新病人、3D、改 Steam 包内容、玩法数值、J/T。

## 回我

四立绘预览（或诊室切换三病人截图）+ commit。
