# 《墨问岐黄》开发任务 V125（针灸入门短环）

日期：2026-09-08  
下发：全局统筹 → 开发负责人 → 小组  
前置：V124 已关；诊室已有针灸榻热区与 `acupuncture.tscn`（点列表选穴）  
依据：设定包 `13_针灸志`：针/灸分流；取穴（穴心 + 经上可容区）；得气节奏；卷一只开 **合谷（针）** + **足三里（灸）**；声明不能替代现实针灸

## 目标

把现有「按钮点穴结算」升级成 **可练的入门短环**，仍接在四诊之后的合法治疗路径：

辨完证 → 选针灸 → 体图取穴（手点，不是纯菜单）→ 合谷走 **针 + 得气节奏**，足三里走 **灸 + 壮数/红晕** → 确认进病程结算。  
开方路径保持不动；针灸仍是开放解之一。

手感要对标「用手量、用节奏得气」，不是点图标领分。

## 范围（锁死）

- **教学锁（卷一）**：运行时默认只开 **合谷 `hegu`（针）** 与 **足三里 `zusanli`（灸）** 两个教学穴。其余穴位可灰显/锁，或铜人练手后再开（切片可只做灰显，不进铜人深度）。
- **取穴**：Q 版体图（前视图即可）上点；每穴有穴心 + 略大的可容区。穴心=准高；可容区=「宁失其穴，勿失其经」小扣；出经=该针无效/稳微扣，可重试。
- **针（合谷）**：点中后短节奏（补慢按轻提 / 泻紧提慢按 / 平匀——切片可先做 **平** 一档）。踩中且在容区内才算得气；得气可有短墨晕。失败可再试 1 次。
- **灸（足三里）**：无得气条；选壮数 **3 / 5 / 7**（默认 5）+ 简单红晕是否圆润（可用短进度/点击确认简化）。
- **数量**：单次处治 1～3 穴即可（超过不加分）；切片教学案至少能「只针合谷」或「只灸足三里」或「针+灸各一」合法过。
- **结算**：沿用现有 `settle("acupuncture", …)`；向量/开放解不改成唯一标准方。禁穴/孕产只做游戏标签提示，不写现实进针教法。
- **文案**：苏问舟 2～3 句教学；页脚/提交前复用文化声明键。穴名：**中文 + 拼音 + 英/日**。中英日 UI。
- **美术**：明快 Q 版体图（可简化剪影升级现 ColorRect）；穴点、墨晕、艾灸红晕、得气条。不要写实解剖暴露。

## 完成标准

1. 从诊室针灸榻/问诊「针灸」能进体图，再退回；开方路径未回退。
2. 合谷可针 + 得气节奏；足三里可灸 + 壮数；默认只开这 2 穴教学。
3. 点偏出经有反馈且可重试；得气/灸成功后能提交并进结算。
4. 冒烟：`SMOKE PASS` + 新增 `acu_intro_smoke_ok`（至少一条：合谷得气 或 足三里灸 → 结算）。
5. 推 main；`secrets.env` 不进库；LLM 不改。

## 分工

### 开发负责人
- 转发本文件；定体图坐标与存档（穴「识/会」可先本地会话级）；手操验收；推送。

### 游戏逻辑设计
- `acupoints` 补：`method`（needle/moxa/both）、`teach_vol1`、`tolerance`、向量沿用。
- 取穴命中判定、得气成功/失败、灸壮数；出经扣稳规则。
- 卷一锁表：非教学穴不可选（或选了提示未开）。
- 评分：针灸路径仍开放解；不因炮制/十八反误伤。

### 剧本设计
- 合谷/足三里教学与成败反馈（中英日）；苏问舟短句；怕针一句可选。
- 不写现实操作教人进针；声明键复用。

### 游戏美工设计
- Q 版体图（前）；合谷/足三里穴点态；得气墨晕；灸红晕；得气/壮数 UI。
- 风格跟现行医馆明快 Q 版。

### 游戏场景设计
- 针灸榻切入体图相机/热区清楚；进出口接诊室。不新开大地图。

### 游戏角色开发
- 升级 `acupuncture.gd`：体图点选 + 得气/灸小环 + 教学锁；冒烟。
- 保留旧按钮列表仅作辅助可选（默认可关）。

### 其他要素 / 音乐（按需）
- i18n：`ACU_*`（HINT、DEQI、MOXA_ZHUANG、MISS_JING、TEACH_LOCK…）。
- 音效：得气/艾灸可复用或补 1～2 条；`needle.ogg` 已有可留。

## 不做

24 穴全开、五行生克弧、铜人阁楼深度、补泻三档全做、针药并用深度 UI、皇甫谧/杨继洲课树、情缘陪练、孕产真实医嘱、改 Steam 包、J/T。

## 回我

手操 30 秒或截图（体图点合谷得气 或 灸足三里 → 结算）+ commit。
