# 《墨问岐黄》开发任务 V127（复诊日循环）

日期：2026-09-08  
下发：全局统筹 → 开发负责人 → 小组  
前置：V126.1 立绘齐套已验收；V120 已有结算写 `play.pending_revisits[]` + 空闲码头一句回访文案  
依据：设定「病人离开进入病程 → 几天后回访」；`logic/slice_logic.json` → `revisit_save`；剧本 `followup` / `followup_by_case`（本地模板，不接 LLM）

## 目标

把「结算后闪一句回访」升级成 **可玩的复诊短环**：

治完一人 →（过一日 / 次日开馆）→ 该病人作为 **复诊号** 再坐上诊椅 → 看病程变化（向愈 / 迁延 / 过治不适）→ 轻量再处治或收工安心 → 清掉对应 pending。

手感：治疗有后果，不是结算完就消失；仍软失败，不 Game Over。

## 范围（锁死）

- **日时钟（切片最小）**：`play.day` 整数即可。结算后提供明确入口「次日开馆 / 过一日」（诊室空闲或结算页按钮）。采药/炮制仍可在同日，不强制重做。
- **触发**：`pending_revisits` 里 `consumed=false` 且 `due_day <= play.day`（无 day 时：次日开馆后到期）。每次开馆最多拉起 **1** 个复诊号（FIFO）。
- **复诊就诊**：
  - 病人再入座（用 V126 立绘）；标题/角标显示「复诊」类键。
  - **轻量四诊**：默认已有上次印象；只需 1～2 个检查或直接听主诉更新（模板句，不接 LLM 也可过验收；若走问诊则用短模板/offline）。
  - 主诉/病程句按上次 `rank_id` / `flavor_kind` 分支：`good|slow|over|mis`（向愈感谢 / 好一点还要调 / 药过了不舒服 / 好像没对上）。
- **再处治**：可选开方或针灸（沿用现路径）；或「观察勿药」收工（向愈档推荐）。提交后再结算一小段，标记该 pending `consumed`。
- **数据**：补齐 `revisit_save.record_shape` 字段（`treated_at_day` / `due_day` / `flavor_kind`）；`due_day = treated_at_day + 1` 即可。
- **文案**：每人/每案 × 4 flavor 的复诊句（中英日）；可扩 `followup_by_case` 或新 `revisit_lines`。苏问舟最多一句疑问旁白。
- **不做大日历 UI**、不做多日挂机模拟、不做小荷代诊复诊。

## 完成标准

1. 治完阿福（或任一案）→ 次日开馆 → 阿福以复诊入座，能看到与疗效相关的病程句。
2. 复诊可再开方/扎针或观察结束；结束后 pending 清除，不重复刷同一条。
3. 存档重进仍能看到未消费的复诊。
4. 冒烟：`SMOKE PASS` + `revisit_day_ok`（settle → day+1 → pending 入座 → consume）。
5. 推 main；LLM 默认策略不改；`secrets.env` 不进库。

## 分工

### 开发负责人
- 转发；定 day 入口与 pending 消费时机；手操验收；推送。

### 游戏逻辑设计
- 补 `due_day` / `flavor_kind`；开馆拉取规则；复诊态 FSM（`revisit_consult`）；观察勿药结算。
- 评分：复诊再治可沿用 evaluate，权重可略降；向愈观察给小稳分即可。

### 剧本设计
- 三案 × 4 flavor 复诊主诉/回访句（中英日）；角标「复诊」；观察收工一句。
- 仍不报诊断名；不接 LLM 也可玩。

### 游戏角色开发
- day+1 入口；pending → 自动/可点入座；复诊 HUD；消费 pending；冒烟。

### 游戏美工设计
- 「复诊」角标/小章；病程条或对话框色差（轻量）；不新立绘。

### 游戏场景设计
- 诊室空闲态挂「次日开馆」热区或按钮位置；不挡四诊。

### 其他要素（按需）
- i18n：`REVISIT_*`、`NEXT_DAY`、`FOLLOWUP_TITLE` 复用或扩。
- 音效：次日开馆门闩/晨钟可复用 1 条。

## 不做

完整时令日历、多病人同日排队 AI、小荷代诊、陈半仙抢诊、跨案病程网、改 Steam、J/T。

## 回我

手操或截图：初诊结算 → 次日 → 复诊入座 → 结束 + commit。
