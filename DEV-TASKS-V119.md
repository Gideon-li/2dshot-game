# 《墨问岐黄》推进任务（v1.19 设定包落地）

日期：2026-09-06  
来源：`design-pack-v1.19/`（正文在 `墨问岐黄_设定文档包_v1.19/`，摘要 `extracted/`）+ Helix API  
工作名可并用：墨问岐黄 / 杏林墨问。切片仓库仍是 `Gideon-li/2dshot-game`。

## 已确认

- Helix 问诊 API 已写入 `secrets.env`（`QWEN_BASE_URL` / `QWEN_API_KEY` / `QWEN_MODEL=qwen3.8-flash`）。禁止进 git、禁止贴聊天。客户端已改为读该 base URL。冒烟：API_OK。
- 风格锚点：`ui/art-anchor-clinic.png`（江晚切赵阿福；旧锚点图仍可用）。`ui/layers/CAM_HERO.png` 已用此图覆盖。
- 切片人设继续：学徒 **江晚**（默认；可改名。旧稿「江晚」一律改称江晚）、病人赵阿福 / 沈清荷 / 周婆婆。师父 **苏问舟** 本期只旁白/UI，不做可点师傅 HUD。
- 玩法锁：病人不交全症状；方剂与针灸都合法；无唯一解；启动免责；不要把大模型当卖点。

## 切片仍只做这些（不要扩全作）

一座杏林小筑诊室 · 江晚 · 3 病人 · 完整望闻问切 · 方/针两条路径 · 疗效分 · 中英日 · 本地存档。  
本期不做：穿越、排行榜、情缘、全地图、收徒经营、百事卷全量。

## 各组任务

### 游戏逻辑设计
读：`16(v1.19)`、`17`、计分引擎、`18+19`（先接口）、`15` 十问。  
交付：
1. 把 `logic/slice_logic.json` 对齐引擎字段：`required_actions` / 君臣权重可后置，但结算要能接 `M = 0.45C* + 0.20B + 0.25A' + 0.10U` 的切片简化版（切片可先不做 J/T，留乘子=1 的接口）。
2. 反畏：开方前硬锁接口（能提示原因即可，全表可后置）。
3. 十问：问诊面板加「十问」快捷条（催四诊），不弹百科。

### 剧本设计
读：`03 人物志`、`15 十问`、切片 `patients/`。  
交付：
1. 江晚 / 苏问舟 / 小荷 / 三病人的问诊身份包（禁忌、口吻、动机）写入 `patients/`。
2. 三案各补 8–10 条问舟旁白边界（不得报诊断名）。
3. 苏问舟只做旁白键值，不进可点角色。

### 游戏美工设计
读：风格锚点 `ui/art-anchor-clinic.png`、`00` 附图说明。  
交付：
1. CAM_HERO 以锚点为准（江晚切阿福）。不要再画「女孩被切脉」。
2. `pulse-overlay.png` 裁掉英文吉祥物，只留三列波形（上浮紧/中弦/右细数）。
3. 药盘 / HUD 跟锚点明快 Q 版统一；经络图只装饰不可点。

### 游戏场景设计
读：锚点 + `SCENE-SLICE.md`。  
交付：医馆 L1–L7 向锚点靠（暖木器、药柜、窗光落脉枕）。相机名和热区不动。

### 游戏角色开发
交付：
1. 确认问诊走 Helix（`secrets.env`），失败回退人物卡模板。
2. 换最新 CAM_HERO / 建筑贴图；重抓 `ui/store/CAM_FORMULA|NEEDLE|RESULT`。
3. 接入逻辑的反畏锁与十问条（有表就接，没有就留钩子）。

### 游戏音乐
读：`05 音乐与音效志`。  
交付：对照现有 `other-systems/audio/`，缺的交互音按志补；仍不按病人切整首 BGM；切诊 duck。

### 其他要素
交付：i18n 键补「十问」「反畏锁定原因」「苏问舟旁白」占位；启动免责保留；Steamworks 仍只预研。

## 完成标准（本轮）

- 手操：免责 → 点阿福 → 四诊（问诊打到 Helix）→ 开方或扎针 → 见疗效分 → 下一位。
- CAM_HERO = 江晚切阿福；商店第 3 仍有脉象。
- `secrets.env` 不在 git。
- 冒烟仍 PASS；另加一次真实问诊 API 回包（不打印密钥）。
- 十问数据表可加载；计分含 C*/B/A/U 接口且 J=T=1 时合法路径仍向愈。
