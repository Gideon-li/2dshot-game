# 工作名变更说明

- **当前工作名（设定包）**：**墨问岐黄**（来自 design-pack-v1.19 包名与总目）
- **既往工作名**：**杏林墨问**（仓库/文件夹 `xinglin-mowen` 仍沿用此名，以保持连续性）
- 在 Haopeng 明确要求改仓库/文件夹名之前，不重命名目录或 git remote；文档与对外文案优先使用 **墨问岐黄**，工程路径可继续写 `xinglin-mowen`。
