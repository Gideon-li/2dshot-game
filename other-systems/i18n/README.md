# 中英日 UI 键值

Godot 工程翻译源：`locale/xinglin.csv`（与本目录 `ui.csv` 同步）。

- 文案走键值，场景里不要写死中文。
- 英日不要写成「学中医」。
- v1.19：`TENQ_*` / `FANWEI_*` / `MENTOR_*`
- v1.20：`MENTOR_SU_*`（剧本 hint）、`XIAOHE_*`（候诊/药柜口吻）、`FOLLOWUP_*`（回访/病程一句）
- 启动免责 `BOOT_DISCLAIMER_*` 保留。Steamworks 仍只预研。
- 病人长对白、案旁白库仍可住在 `patients/slice_characters.json`；表内键供 HUD/气泡 `tr()`。
