# 中英日 UI 键值

Godot 工程翻译源：`locale/xinglin.csv`（由本目录 `ui.csv` 同步键值；工程以 xinglin 为准加载）。

- 文案走键值，场景里不要写死中文。
- 英日不要写成「学中医」。
- v1.19 增补：`TENQ_*` 十问条、`FANWEI_*` 反畏锁定原因、`MENTOR_*` 苏问舟旁白占位。
- 启动免责 `BOOT_DISCLAIMER_*` 保留。Steamworks 仍只预研，无成就键。
- 病人对白、病机、药名不在此表。反畏长因（医典页）可后置，切片先用 `FANWEI_SHORT_*`。
