# 《墨问岐黄》开发任务 V124（炮制小游戏）

日期：2026-09-08  
下发：全局统筹 → 开发负责人 → 小组  
前置：V123 Coming Soon 包已验收；采药环（V121 / V121.1）已在可玩日  
依据：设定包 01「黄昏炮制」、炮制院（炉/晒/洗）；04「附子、半夏、杏仁须先完成炮制教学才能入盏」；剂量/毒性游戏化用「份、火候」

## 目标

在可玩日接上 **采药 → 黄昏炮制 → 回药房开方** 短环：

药圃采完（或午后空档）→ 进 **炮制院** → 对 2～4 味相关药做洗 / 炒（火候）/ 晒 中的一种操作 → 得到 **炮制态** 饮片 → 回托盘可用；未炮制的「须炮制」药锁死不能入盏，并给一句苏问舟教学。

手感要对标「用手做」：看火候条或洗涮节奏，不是点菜单领成品。文化声明与毒性警告要克制、清楚。

## 范围（锁死）

- 地点：杏林小筑 **炮制院**（可做诊室/药房旁一小间或药房一角扩展）。不要五方大地图、不要天气改写深度。
- 药味：从现有白名单里挑 **2～4 味**，必须含至少 1 味「须炮制才能入盏」的教学药（优先 **杏仁**；可选 **附子** 作带警告的教学关，须有安全替代路径——切片里可不强制用附子治病）。
- 玩法（选做 2 种即可，第三种可占位）：
  1. **洗**：按节奏点/拖，去泥去尖（杏仁去皮尖可用此表现）。
  2. **炒 · 火候**：滑动或点按保持火候条在绿区；过火/欠火降低品质，不 Game Over。
  3. **晒**：可选短时进度（或一键置半日占位），切片可简化为进度条 + 小荷一句。
- 数据：每味药补 `needs_process` / `process_method` / `raw_id`→`processed_id`（或同 id + `state: raw|processed`）。托盘只收 `processed`（或 `needs_process=false`）。
- 评分：未炮制却强行入盏 → 拦截提交 + 扣稳/安全分；炮制品质影响小幅加成即可（先简单两档：妥 / 勉强）。
- 时辰：耗 **暮** 格 1（或夜格占位），与现有一日格对齐即可。
- 文案：苏问舟教学 2～3 句；小荷吐槽 1～2 句；毒性药明确警告 + 页脚文化声明键。中英日。
- 美术：明快 Q 版；炉、盆、晒架、火候 UI；不要写实解剖/血腥。

## 完成标准

1. 从诊室/药房能进炮制院，再返回；流程能接在采药之后。
2. 至少 **2** 种炮制操作可玩；至少 **2** 味药能从 raw → processed。
3. 须炮制药未加工时 **不能** 拖进托盘（或拖入即无法提交并提示）；加工后可入盏。
4. 冒烟：`SMOKE PASS` + 新增 `process` 冒烟（洗或炒一轮 → 托盘可选 → 开方仍可结算）。
5. 推 main；`secrets.env` 不进库；不改玩家默认远程 LLM（仍走 V122 本地优先）。

## 分工

### 开发负责人
- 转发本文件；定场景切入点与存档字段（`inventory[].state` 或 processed 库存）；手操验收；推送。

### 游戏逻辑设计
- `herbs` / forage 表补炮制字段；品质两档规则；入盏拦截；时辰消耗。
- 附子若进切片：必须 `warning` + 安全替代说明；儿童/孕产标签仍禁有毒向量（沿用既有锁）。
- 十八反不因炮制解锁（制附子仍反半夏）——沿用现锁表。

### 剧本设计
- 教学/警告/成败反馈文案（中英日）；苏问舟「火候在人，不在炉」类短句；小荷口吻。
- 不写真实克数与可替代就医话术；页脚声明复用既有键。

### 游戏美工设计
- 炮制院背景（炉、水盆、晒架）；火候条 / 洗涮 UI；2～4 味 raw vs processed 图标差分（可简单色变+标记）。
- 风格跟现行医馆明快 Q 版。

### 游戏场景设计
- 炮制院空间与 1～2 个工位热区；进出口接药房或诊室。
- 相机简单，热区清楚。

### 游戏角色开发
- 小游戏状态机、库存态切换、托盘拦截、冒烟。
- Helix / 本地 LLM 不改。

### 其他要素 / 音乐（按需）
- i18n 键：`PROCESS_*`、`PROCESS_WARN_TOXIC`、`PROCESS_DONE_HINT`。
- 音效：洗水、炒药、出锅 1～2 条可复用或新录短音。

## 不做

五方旅行、完整毒理树、马钱子等更高阶毒、种植养成、自动半日挂机深度、针灸炮制联动、情缘、穿越、改 Steam 包、J/T。

## 回我

手操 30 秒或截图（炮制院操作 + 加工后入盏）+ commit。
