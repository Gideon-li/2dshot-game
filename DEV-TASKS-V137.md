# 《墨问岐黄》开发任务 V137（诊室 UI 布局 · 设置入口 · 对话滚底）

日期：2026-09-24  
下发：全局统筹 → 开发负责人 → 小组  
前置：V135 已在 main；V136 可并行（本档不阻塞 V136 证印/立绘）  
来源：Haopeng 手操截图 `incoming/ui-overlap-haopeng-20260924.png`（窗口仍标「杏林墨问 (DEBUG)」；候诊牌盖住标题与旁白；右上语言三钮与「去药圃/去炮制院」叠在一起）

## 目标

把诊室从「调试按钮堆」收成可演示的基础壳：

1. **布局不叠** — 标题、候诊 A–D 牌、场景旁白、对话面板、导航互不遮挡。  
2. **设置入口** — 语言（中/英/日）与存读档等基础功能进 **设置**（齿轮或「设置」钮），不再常驻三颗语言大钮。  
3. **对话滚底** — 新消息追加后聊天区自动滚到最新一行，无需玩家手动下拉。

## 范围（锁死）

### A. 布局分区（clinic / consult HUD）

- 顶栏：游戏名（正式名优先「墨问岐黄」；DEBUG 角标可保留但不占主标题位）与 Day/时辰不与候诊牌抢位。  
- 候诊牌固定在一侧或顶条专用带，**不得**盖住标题字与底图旁白。  
- 「去药圃」「去炮制院」等场景跳转收入统一导航区（底栏或侧栏），与语言按钮解耦。  
- 对话面板（望闻问切 + 十问 + 输入）保持左下/下半可读区，不与候诊牌重叠。  
- 1280×720（或当前导出分辨率）手操一遍：标题、四席、对话、导航均完整可见。

### B. 设置面板

- 常驻一枚 **设置** 按钮（与存档同级心智模型）。  
- 面板内至少：语言切换（中/英/日，沿用 `Save.set_locale` / `GameFlow.set_locale`，切语言不丢进度）、**保存**、**读档**（若已有 slot API 则接上；没有则先做保存 +「已写入」反馈，读档可接现有 slot）。  
- 关掉面板回游戏；Esc 或二次点击可关。  
- 去掉（或默认隐藏）诊室常驻的三颗语言大钮；DEBUG 快捷可进设置或 `DEBUG` 折叠。

### C. 对话自动滚底

- `clinic.gd` / `consult.gd` 里对话 `ScrollContainer`（或等价）：每次追加苏问舟/病人行、十问回复、系统句后 `scroll_vertical = max`（或 `ensure_control_visible` 最后一行）。  
- 玩家正在上翻阅读时：可选「有新消息」小提示 + 点一下再贴底；默认策略 **新消息强制贴底**（Haopeng 明确要求聚焦最新）。  
- 切换病人 / 开新诊时滚到底（空日志也贴底）。

### D. 冒烟

- `ui_chrome_ok`（可 headless 或轻手操日志）：打开设置→切 locale 一次→写档一次；追加 ≥3 行对话后 scroll 在底部。  
- 原有 `SMOKE PASS` / `demo_day_ok` 不回退。

## 完成标准

1. 对照 `incoming/ui-overlap-haopeng-20260924.png`：同机位不再出现「候诊牌压标题/旁白」「语言与去药圃叠成一摞」。  
2. 语言仅从设置进出；保存可用。  
3. 连续问三句十问或自由问，聊天区始终停在最新句。  
4. 不新开病例/证印/出镇地图；不改 LLM/secrets；不重做整套美术皮肤（可微调锚点与容器）。  
5. 推 main；回布局前后对照截图（或短录）+ commit。

## 分工

### 开发负责人
- 转发；定顶栏/设置/对话容器方案；手操对照截图验收；推送。

### 游戏角色开发 / 其他要素
- clinic/consult HUD 容器与锚点；设置面板接 Save；对话滚底；`ui_chrome_ok`。

### 游戏美工设计
- 设置齿轮/面板小铬（明快 Q，复用现有米色面板风格）；少新大图。

### 游戏逻辑设计
- 仅在需要时补「设置开闭」状态；不改病机。

### 剧本设计
- 设置内短文案键（`SETTINGS_*` / `SAVE_*`）中英日；不问诊剧透。

### 游戏场景设计
- 确认候诊热区与牌位对齐后仍可点。

## 不做

V136 立绘/证印抢活、五方、情缘、Steamworks、整页 UI 换皮、改默认 LLM。

## 回我

布局前后对照（或同机位新截）+ 设置开合一句 + 对话滚底确认 + `ui_chrome_ok` / commit。
