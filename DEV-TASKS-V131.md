# 《墨问岐黄》开发任务 V131（Demo 打磨 · 可玩日串联）

日期：2026-09-16  
下发：全局统筹 → 开发负责人 → 小组  
前置：V119–V130 各玩法环已进 main（诊/方/针/食疗、采、炮含晒、复诊、夜读、立绘、Coming Soon 素材）  
问题：环在，但 Demo 仍像「一堆按钮」；缺一条跟得住的 **一日动线**、时辰可见性、卡点提示与端到端冒烟。

## 目标

做出 **一条可演示的完整日**（约 8～15 分钟可讲完）：

开馆免责 → 接一诊（阿福或任意）→ 四诊 → 处治（方 **或** 针 **或** 食疗）→ 午后药圃认采 → 黄昏炮制（洗/炒/晒任一事）→ 夜读一页医典 → 次日开馆 → 复诊入座看病程 → 观察或再治收工。

打磨重点是 **串联与引导**，不是新玩法大系统。

## 范围（锁死）

### A. 一日动线胶水

- 诊室空闲 HUD 显示：`Day N` + 当前可用行动（晨诊 / 药圃 / 炮制暮 / 阁楼夜 / 次日）灰亮逻辑与 `time_slots` 一致。
- **首日软引导**（可关）：侧栏或底条 6 步 checklist，完成打勾；不强制顺序卡死（可跳步），但推荐序如上。
- 结算后明确下一跳文案（「可去药圃采药」「暮可进炮制院」「夜可上阁楼」），避免玩家愣住。
- 暮格/夜格用尽时按钮灰 + 一句原因；「提前歇息入夜」保留。
- 次日开馆后 checklist 重置日段；未消费复诊优先提示。

### B. Demo 打磨（挑痛点修）

- 启动免责仍在；语言切换中英日不丢进度。
- 死路排查：从任意子场景（园/炮/阁/食疗/针）都能回诊室空闲；返回不吞存档。
- 立绘/角标（复诊）在引导日路径上可见。
- 已知小字/缺键补齐（引导条、Day、slot 提示）。
- 可选：主菜单或启动加「Demo 一日」快捷（新档直接诊所 + 引导开）。

### C. 端到端冒烟

- 新增 `demo_day_ok`（可 headless）：  
  选人→最少四诊→settle（任一路）→ go_garden 采 1→ go_process 处理 1→ loft 解锁 1 页→ advance day→ pending 复诊入座→ observe/consume。  
- 原有各环 smoke 仍 PASS。

## 完成标准

1. 新人按引导能不查 wiki 走完上述一日（内部手操 1 次录像或逐步截图）。
2. HUD 时辰/日数正确；格用尽有反馈。
3. `demo_day_ok` + `SMOKE PASS`。
4. 不新开五方/情志/经济；不改 LLM 默认；secrets 不进库。
5. 推 main；更新 `README` 或 `DEMO-DAY.md` 一页怎么玩。

## 分工

### 开发负责人
- 转发；定引导开关默认开；手操整日验收；推送；写 `DEMO-DAY.md`。

### 游戏逻辑设计
- `time_slots` 与按钮可用性单一真相；checklist 进度存 `play.demo_guide`。
- `demo_day_ok` 脚本步骤与失败信息。

### 剧本设计
- 引导 6 步短文案 + 结算后下一跳句（中英日）；不问诊剧透证型。

### 游戏角色开发
- HUD 日/格；checklist UI；结算下一跳；子场景返回健壮；冒烟串联。

### 游戏美工设计
- 引导条/勾选小印；Day 小牌；尽量复用现 UI 铬，少新大图。

### 游戏场景设计
- 确认各入口热区在引导日都可发现（门/园/炮/阁标签清晰）。

### 其他要素
- i18n：`DEMO_*` / `DAY_*` / `SLOT_*`；`DEMO-DAY.md`。

## 不做

新病例、新地域、情志疏导、Steamworks 上架、重做 Coming Soon、大数值重做、J/T。

## 回我

整日手操要点（或截图条）+ `demo_day_ok` 日志 + commit。
